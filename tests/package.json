{
  "name": "@apite/sw6-utility-tests",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "full": "node_modules/.bin/newman run -r cli,htmlextra ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --reporter-htmlextra-export postman-report.html --color=on --bail",
    "ci": "node_modules/.bin/newman run -r cli,htmlextra,junit ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --color=on --insecure --no-insecure-file-read"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "latest",
    "newman-reporter-htmlextra": "latest"
  }
}
