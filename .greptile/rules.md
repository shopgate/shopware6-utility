## Deployment
### Publishing to NPM
The package uses trusted publishing for NPM packages and due to that feature doesn't need a token. See https://docs.npmjs.com/trusted-publishers for more information.